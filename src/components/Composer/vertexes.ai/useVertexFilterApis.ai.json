{
    "name": "useVertexFilterApis",
    "description": "This function, named useVertextFilterApis, is designed to sift through a collection of APIs, each defined within individual JSON files. Its primary goal is to identify and return the set of APIs that align most closely with a provided prompt.",
    "input": {
        "type": "object",
        "properties": {
            "rawInput": {
                "type": "string"
            }
        },
        "required": [
            "rawInput"
        ],
        "additionalProperties": false
    },
    "output": {
        "type": "object",
        "properties": {
            "filteredApis": {
                "type": "array",
                "items": {
                    "type": "string"
                }
            }
        },
        "required": [
            "filteredApis"
        ],
        "additionalProperties": false
    }
}